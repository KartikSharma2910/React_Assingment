import { SxProps } from "@mui/system";

const styles: {
  [key: string]: SxProps;
} = {
  triggers: {
    width: "20%",
    padding: "1rem",
    backgroundColor: "blue",
    borderRadius: "10px",
    color: "white",
    fontSize: "1.5rem",
    margin: "5px",
  },
};

export default styles;
